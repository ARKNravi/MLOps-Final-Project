version: "2"
authtoken: ${NGROK_AUTHTOKEN}

tunnels:
    grafana:
        addr: 3000
        proto: http

    prometheus:
        addr: 9090
        proto: http

    loki:
        addr: 3100
        proto: http
